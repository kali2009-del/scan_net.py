scan_net.py
A Python-based port scanning tool that utilizes the Nmap library to scan a target IP address within a specified port range. It detects open, closed, or filtered ports and identifies the associated network services.
